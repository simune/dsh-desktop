# 本工作区位于 ExFAT 卷(/Volumes/Data),会为每个文件生成 ._ AppleDouble 侧车,
# 导致 tauri-build 遍历输出目录时 panic。将 cargo 构建产物移到系统 APFS 卷。
# 个人项目固定路径;若迁移到 APFS 卷可删除本文件恢复默认 target 目录。
[build]
target-dir = "/Users/lm/Library/Caches/dsh-desktop/target"
